# Interactive-SHACL-Shapes-Graph-Editor
A visual, interactive editor for SHACL shapes graphs. The editor represents the shapes graph as an interactive tree — mirroring the hierarchical structure of NodeShapes, PropertyShapes, and constraints — and allows users to manipulate that structure directly through familiar editing operations, without touching the raw Turtle syntax.
